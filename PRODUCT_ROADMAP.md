# easy_resume 功能规划文档

本文档记录 easy_resume 后续可以扩展的功能方向。规划原则是：先把在线简历编辑器的核心体验做稳，再逐步增加 AI、模板、多版本、分享等能力。

核心目标：

- 写得快：用户可以快速生成一份完整简历。
- 改得准：内容能针对岗位和简历规范持续优化。
- 排版稳：编辑、预览、导出 PDF 的结果一致。
- 导出不翻车：导出前能发现空内容、溢出、示例文本等问题。
- 能针对岗位优化：通过 AI 和规则让简历更贴合目标 JD。

## 1. 当前基础能力

项目目前已经具备以下能力：

- 简历画布编辑。
- 基础信息模块。
- 段落模块。
- 富文本编辑。
- 简历模板初始数据。
- 主题样式调整。
- 本地存储。
- PDF 导出。
- AI 简历点评。
- 移动端和桌面端编辑面板。

这些能力已经构成在线简历编辑器的基础。后续功能应优先围绕“编辑稳定性、内容质量、导出质量、模板复用”展开。

## 2. 第一优先级：先把核心编辑体验做稳

### 2.1 富文本编辑稳定性

目标：

- 点击段落编辑时不丢内容。
- 第一次输入立即生效。
- 切换段落时能正确加载对应内容。
- 编辑器销毁和重建不会造成内存泄漏。

建议：

- 保持富文本编辑器非受控。
- 使用 `initialValue` 只在创建时初始化内容。
- 通过 `paragraphId` 控制编辑器重建。
- 初始化阶段只跳过 WangEditor 自身触发的同步事件，不吞掉用户第一次输入。

价值：

- 这是所有段落编辑的基础。
- 直接影响用户对工具可靠性的信任。

### 2.2 Undo / Redo 真正可用

目标：

- 支持 `Ctrl + Z` 撤销。
- 支持 `Ctrl + Y` 或 `Ctrl + Shift + Z` 重做。
- 支持文本、样式、拖拽、排序、新增、删除的历史记录。

建议：

- 历史栈保存深拷贝快照，不保存会被继续 mutation 的引用。
- 文本编辑历史应防抖保存，避免每输入一个字都入栈。
- 拖拽只在 mouseup 后保存一次历史。
- 历史栈限制最大长度，例如 50 步。

价值：

- 编辑器类产品的基础能力。
- 用户敢于尝试样式和内容修改。

### 2.3 自动保存和恢复

目标：

- 自动保存当前简历。
- 显示保存状态和保存时间。
- 页面刷新后恢复上一次内容。
- 清空数据前二次确认。

建议：

- 将 `localStorage` 读写封装到 `useResumePersistence`。
- UI 上显示类似“已自动保存 14:32”。
- 保存失败时提示用户。
- 后续可从 `localStorage` 升级到 `IndexedDB`。

价值：

- 增强安全感。
- 降低误关闭页面导致内容丢失的风险。

### 2.4 导出前检查

目标：

在下载 PDF 前检查明显问题。

可检查项：

- 是否存在空段落。
- 是否存在示例文本未替换，例如“张三”“XXX 公司”“13333333333”。
- 手机号格式是否合理。
- 邮箱格式是否合理。
- 内容是否超出 A4 页面。
- 是否有重复模块。
- 是否缺少联系方式。
- 是否缺少求职意向。
- 是否缺少核心经历模块。

价值：

- 实现成本相对低。
- 对最终导出质量帮助很大。
- 能减少用户导出一份明显有问题简历的概率。

### 2.5 页面溢出提示和自动分页

目标：

- 检测内容是否超出 A4 页面。
- 显示溢出高度。
- 提供一键压缩建议。
- 后续支持自动分页。

建议：

- 基于画布高度和元素底部位置计算溢出。
- 超出时显示红色页面边界提示。
- 给出压缩建议：
  - 减小段落间距。
  - 减小正文字号。
  - 减小行高。
  - 删除空段落。
  - 自动分页。

价值：

- 简历工具非常关键。
- PDF 导出前能显著减少排版翻车。

## 3. 第二优先级：内容质量和 AI 能力

### 3.1 AI 单段优化

目标：

用户可以对某一段经历或某一条描述进行 AI 优化。

可选操作：

- 优化表达。
- 改成 STAR 法则。
- 增强量化结果。
- 压缩到 3 条 bullet。
- 转成更专业的简历语言。
- 检查错别字。
- 改成英文。
- 改成面试官更容易理解的表达。

示例：

原文：

```text
负责后台页面开发，完成订单管理功能。
```

优化后：

```text
负责订单管理模块的前端开发，完成订单查询、状态筛选、详情查看与批量操作等核心功能，提升运营人员处理订单的效率。
```

价值：

- 比整份点评更直接。
- 用户可以一键替换当前段落。
- 能明显提升简历内容质量。

### 3.2 简历评分系统

目标：

对当前简历进行规则和 AI 结合的评分。

评分维度：

- 信息完整度。
- 岗位匹配度。
- 项目描述质量。
- 是否有量化成果。
- 是否有动作动词。
- 是否存在空话套话。
- 是否有错别字。
- 是否排版溢出。
- 是否超过一页。
- 是否缺少联系方式。

示例输出：

```text
综合评分：82 / 100

优势：
- 项目经历完整。
- 技能栈覆盖较清晰。
- 排版整体紧凑。

建议：
- 项目经历缺少量化结果。
- 技能栈可以按熟练程度分类。
- 个人优势部分略泛泛。
```

价值：

- 比纯聊天式 AI 更产品化。
- 能让用户知道下一步该改哪里。

### 3.3 根据岗位 JD 优化简历

目标：

用户粘贴目标岗位 JD，系统分析简历匹配度并给出优化建议。

能力：

- 提取 JD 关键词。
- 对比当前简历内容。
- 标记已匹配能力。
- 标记缺失能力。
- 给出修改建议。
- 对相关段落进行 AI 改写。

示例：

```text
匹配度：76%

已匹配：
- React / TypeScript
- 中后台项目经验

缺失：
- 性能优化描述不足
- 工程化经验没有展开
- 项目结果缺少量化指标
```

价值：

- 非常贴近求职场景。
- 适合做成项目的核心差异化能力。

### 3.4 AI 生成项目描述

目标：

帮助用户从简单信息生成专业的项目经历。

输入字段：

- 项目名称。
- 你的角色。
- 使用技术。
- 主要功能。
- 你解决的问题。
- 最终结果。

输出风格：

- 简洁版。
- 专业版。
- 量化版。
- STAR 版。
- 面试重点版。

价值：

- 解决用户“不知道项目经历怎么写”的问题。
- 对应届生和初级开发者尤其有用。

### 3.5 AI 面试问题生成

目标：

根据当前简历生成面试问题和参考回答。

能力：

- 根据项目经历生成追问。
- 根据技能栈生成技术问题。
- 根据工作经历生成行为面试问题。
- 给出参考回答思路。
- 标记高频问题。

价值：

- 形成“写简历 -> 优化简历 -> 导出简历 -> 准备面试”的闭环。

### 3.6 AI 风险扫描

目标：

识别简历中的潜在风险。

检查项：

- 时间线是否冲突。
- 技能是否写得过满但项目没有体现。
- 项目描述是否太空。
- 是否有夸大嫌疑。
- 是否包含敏感个人信息。
- 是否有错别字。
- 中文标点是否混乱。
- 英文大小写是否规范。

价值：

- 帮用户提前规避面试官可能质疑的问题。

## 4. 第三优先级：模板、主题和结构化数据

### 4.1 简历数据结构化编辑

目标：

将简历内容从纯 HTML 和绝对定位数据中逐步拆成结构化数据。

基础信息字段：

- 姓名。
- 手机。
- 邮箱。
- 年龄。
- 城市。
- 求职岗位。
- 工作年限。
- GitHub。
- 博客。
- 个人网站。

经历模块：

- 教育经历。
- 工作经历。
- 项目经历。
- 技能清单。
- 个人优势。
- 荣誉证书。
- 开源项目。
- 校园经历。
- 实习经历。

价值：

- 支持模板切换不丢内容。
- 支持 AI 分析更准确。
- 支持导入导出更可靠。

### 4.2 模板切换不丢内容

目标：

用户填写完内容后，可以切换不同模板而不丢简历内容。

建议架构：

```text
resumeContent 用户内容数据
+
templateConfig 模板布局配置
=
resumeData 最终画布数据
```

模板类型：

- 极简黑白。
- 蓝色商务。
- 互联网风格。
- 应届生模板。
- 前端工程师模板。
- 后端工程师模板。
- 产品经理模板。
- 英文简历模板。
- 双栏模板。

价值：

- 这是简历编辑器的核心卖点之一。
- 能明显提升产品完成度。

### 4.3 主题系统

目标：

在不切换完整模板的情况下快速调整视觉风格。

可配置项：

- 主色。
- 标题背景色。
- 字体。
- 正文字号。
- 标题字号。
- 段落间距。
- 行高。
- 分割线样式。
- 页面边距。

主题示例：

- 黑白经典。
- 深蓝商务。
- 绿色清爽。
- 灰色极简。

价值：

- 比完整模板切换更轻。
- 实现成本低于模板系统。

### 4.4 模板库

目标：

提供多个可直接使用的简历模板。

分类：

- 经典单页。
- 极简黑白。
- 商务风格。
- 技术岗。
- 应届生。
- 产品经理。
- 运营。
- 设计师。
- 英文简历。
- 双栏模板。

建议：

- 简历模板不要过度花哨。
- 优先保证专业、清晰、易读。
- 模板应基于结构化数据渲染，而不是复制一份完整 `resumeData`。

## 5. 第四优先级：导入、导出和分享

### 5.1 Markdown / JSON 导入

目标：

支持用户导入已有 Markdown 或 JSON 简历。

Markdown 示例：

```markdown
# 张三

前端开发工程师 / 3 年经验

## 技能栈

- React / Vue / TypeScript
- Next.js / Node.js

## 项目经历

### XXX 后台管理系统
前端开发工程师 / 2024.01 - 2025.01

- 负责订单管理模块开发
- 接入权限系统
```

价值：

- 对程序员用户友好。
- 方便从已有简历迁移到编辑器。

### 5.2 多格式导出

目标：

除 PDF 外支持更多导出格式。

格式：

- PDF。
- PNG 长图。
- Markdown。
- JSON。
- HTML。
- DOCX。

优先级：

1. PDF：正式投递。
2. Markdown：程序员友好。
3. JSON：数据备份和迁移。
4. DOCX：适配传统投递场景。
5. PNG / HTML：分享和展示。

价值：

- 覆盖更多投递和备份场景。

### 5.3 在线分享链接

目标：

用户可以生成一个公开简历链接。

能力：

- 在线查看简历。
- 手机端适配。
- 下载 PDF。
- 设置访问密码。
- 设置过期时间。
- 统计访问次数。

示例：

```text
https://easy_resume.srhui.top/r/abc123
```

价值：

- 将工具从本地编辑器扩展为在线简历服务。
- 适合放在个人主页、GitHub、招聘平台。

### 5.4 简历版本管理

目标：

用户可以保存多份简历版本。

能力：

- 新建版本。
- 复制版本。
- 重命名版本。
- 删除版本。
- 设置默认版本。
- 查看更新时间。

版本示例：

- 前端开发版。
- 全栈开发版。
- 校招版。
- 社招版。
- 字节投递版。
- 腾讯投递版。
- 英文版。

价值：

- 适合用户针对不同岗位投递。
- 可以先基于 `IndexedDB` 本地实现，后续再接云端。

## 6. 第五优先级：程序员简历特色功能

### 6.1 GitHub 信息导入

目标：

通过 GitHub 用户名导入开源相关信息。

可导入内容：

- GitHub 链接。
- 活跃仓库。
- Star 数。
- 主要语言。
- 开源项目 README 摘要。
- 贡献记录。

生成模块：

- 开源项目。
- 技术影响力。
- 个人主页链接。

价值：

- 对程序员简历非常有特色。
- 可以和 AI 项目描述生成结合。

### 6.2 技能栈智能分类

目标：

用户输入技能后自动分类。

输入：

```text
React Vue TypeScript Node.js MySQL Docker Nginx
```

输出：

```text
前端：React、Vue、TypeScript
后端：Node.js
数据库：MySQL
工程化：Docker、Nginx
```

还可以按熟练程度分类：

```text
熟练：React、TypeScript
掌握：Node.js、Docker
了解：MySQL、Nginx
```

价值：

- 让技能模块更清晰。
- 降低用户组织技能栈的成本。

### 6.3 项目关键词检测

目标：

根据目标岗位检测简历中是否包含关键能力。

关键词示例：

- React。
- Vue。
- TypeScript。
- Next.js。
- 性能优化。
- 工程化。
- CI/CD。
- 微前端。
- 组件库。
- 低代码。
- 权限系统。
- 中后台。

价值：

- 帮用户发现简历和岗位之间的能力表达缺口。

## 7. 第六优先级：高级产品化能力

### 7.1 登录和云端同步

目标：

支持用户登录后云端保存简历。

登录方式：

- GitHub 登录。
- 微信登录。
- 邮箱登录。

能力：

- 云端保存。
- 多设备同步。
- 分享链接管理。
- 简历版本管理。

建议：

- 不建议太早做。
- 先把单机编辑体验打磨稳定，再做账号系统。

### 7.2 IndexedDB 本地数据库

目标：

在没有账号系统前，用浏览器本地数据库支持多份简历和历史版本。

适合存储：

- 多份简历。
- 历史版本。
- 模板配置。
- 导入记录。
- 草稿。

价值：

- 比 `localStorage` 更适合富文本和多版本数据。
- 可作为云同步前的过渡方案。

### 7.3 导入 PDF / DOCX 简历

目标：

用户上传已有 PDF 或 Word 简历后，系统解析为可编辑数据。

能力：

- 解析基础信息。
- 解析教育经历。
- 解析工作经历。
- 解析项目经历。
- 解析技能栈。
- 转换成当前编辑器结构。

价值：

- 吸引已有简历的用户迁移。
- 难度较高，适合后期做。

## 8. 编辑效率增强

### 8.1 快捷键

建议支持：

- `Ctrl + Z`：撤销。
- `Ctrl + Y`：重做。
- `Ctrl + Shift + Z`：重做。
- `Ctrl + S`：保存。
- `Delete`：删除选中元素。
- `Esc`：取消选中。
- `Ctrl + D`：复制模块。
- 方向键：微调基础信息位置。
- `Shift + 方向键`：大步移动。

价值：

- 提升编辑效率。
- 让产品更像专业编辑器。

### 8.2 对齐辅助线增强

建议支持：

- 左对齐。
- 右对齐。
- 顶部对齐。
- 底部对齐。
- 水平居中。
- 垂直居中。
- 等间距提示。
- 吸附阈值配置。
- 显示参考线。

价值：

- 改善基础信息模块拖拽体验。

### 8.3 模块复制和样式复用

建议支持：

- 复制基础信息字段。
- 复制段落模块。
- 复制项目经历项。
- 仅复制样式。
- 仅复制内容。
- 应用当前标题样式到所有段落。
- 应用当前正文样式到所有正文。

价值：

- 实现成本不高。
- 对编辑效率提升明显。

### 8.4 拖拽排序段落

目标：

支持通过拖拽调整模块和条目顺序。

可拖拽对象：

- 项目经历条目。
- 工作经历条目。
- 段落模块。
- 技能条目。

建议：

- 可考虑使用 `@dnd-kit`。
- 注意不要和画布拖拽逻辑冲突。

## 9. 页面和视觉设置

### 9.1 页面设置

建议支持：

- A4 / Letter。
- 页面边距。
- 内容宽度。
- 正文字号。
- 行高。
- 段间距。
- 是否显示页码。
- 是否压缩到一页。

价值：

- 满足高级用户精细排版需求。

### 9.2 字体设置

建议支持：

- 系统默认字体。
- 思源黑体。
- 阿里巴巴普惠体。
- Noto Sans SC。
- 英文 serif 字体。
- 英文 sans 字体。

注意：

- PDF 导出时需要确保服务端也能加载对应字体。

### 9.3 简历隐私模式

目标：

用户截图或分享时可以隐藏敏感信息。

能力：

- 隐藏手机号。
- 隐藏邮箱。
- 隐藏地址。
- 姓名脱敏。
- 公司名脱敏。

示例：

```text
张三 -> 张*
13333333333 -> 133****3333
xxx@qq.com -> x***@qq.com
```

价值：

- 适合截图分享和公开展示。

## 10. 新手体验

### 10.1 新手引导

建议：

- 第一次打开时显示轻量提示。
- 提示用户点击左侧模块可编辑。
- 提示右侧可以修改内容和样式。
- 提示基础信息可以拖动。
- 提示可以导出 PDF。

注意：

- 不要做很重的教程。
- 用局部提示即可。

### 10.2 空状态和示例内容

建议：

新增模块时提供更好的示例内容。

项目经历示例：

```text
项目名称：
你的角色：
项目描述：
- 负责 xxx 模块开发。
- 优化 xxx 流程。
- 提升 xxx 指标。
```

技能栈示例：

```text
前端：React、Vue、TypeScript
工程化：Vite、Webpack、Git
后端：Node.js、Express
```

价值：

- 降低新用户不知道怎么填写的门槛。

## 11. 推荐开发顺序

### 阶段一：打稳编辑器基础

目标：让用户放心编辑和导出。

任务：

1. 修好富文本编辑稳定性。
2. 重写 undo / redo。
3. 自动保存提示。
4. 页面溢出检测。
5. 导出前检查。
6. 简历版本本地保存。

### 阶段二：做出内容优化能力

目标：让用户写出更好的简历。

任务：

1. AI 单段优化。
2. 简历评分。
3. JD 匹配分析。
4. AI 生成项目经历。
5. AI 风险扫描。

### 阶段三：完善模板和数据模型

目标：让用户一份内容适配多种模板。

任务：

1. 简历内容结构化。
2. 模板切换不丢内容。
3. 主题系统。
4. 模板库。
5. 页面设置。

### 阶段四：产品化和分享

目标：从本地工具变成在线简历服务。

任务：

1. Markdown / JSON 导入。
2. 多格式导出。
3. 多版本简历。
4. 在线分享链接。
5. 登录和云同步。

### 阶段五：程序员简历特色

目标：形成差异化。

任务：

1. GitHub 信息导入。
2. 技能栈智能分类。
3. 项目关键词检测。
4. 根据简历生成面试题。

## 12. 最推荐优先做的 5 个功能

### 1. 模板切换不丢内容

原因：

- 是简历编辑器的核心卖点。
- 用户填完内容后自然会想换模板。
- 需要尽早调整数据模型。

### 2. AI 单段优化

原因：

- 比整体点评更直接。
- 用户可以立刻看到内容质量提升。
- 适合放在每个段落项旁边。

### 3. 导出前检查

原因：

- 成本相对低。
- 对最终 PDF 质量帮助大。
- 能避免空内容、示例文本、溢出等明显问题。

### 4. 自动分页 / 超出提示

原因：

- 简历编辑器必须解决 A4 页面边界问题。
- 能显著减少导出翻车。

### 5. 多版本本地保存

原因：

- 用户会针对不同岗位准备不同版本。
- 可以先不做账号系统，基于 IndexedDB 本地实现。

